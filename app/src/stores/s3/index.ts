import { bucketCreator } from "./bucket-creator";

export const botBannerBucket = bucketCreator("bot-banners");
export const botIconBucket = bucketCreator("bot-icons");
export const serverIconBucket = bucketCreator("server-icons");
export const serverBannerBucket = bucketCreator("server-banners");
export const userAvatarBucket = bucketCreator("user-avatars");
export const userBannerBucket = bucketCreator("user-banners");
