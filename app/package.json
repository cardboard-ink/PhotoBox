{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "format": "bunx @biomejs/biome format --write ./src",
    "check-format": "bunx @biomejs/biome format ./src",
    "lint": "bunx @biomejs/biome lint --write ./src",
    "check-lint": "bunx @biomejs/biome lint ./src",
    "check": "bunx @biomejs/biome check ./src",
    "check-fix": "bunx @biomejs/biome check --write ./src"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@elysiajs/swagger": "^1.3.0",
    "chalk": "^5.3.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
